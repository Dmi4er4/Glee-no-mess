#pragma once

namespace guest_traits {
  enum class SkinColor {
    kBlack,
    kWhite,
    kCount
  };

  enum class Sex {
    kMale,
    kFemale,
    kCount
  };

  enum class EyeColor {
    kGreen,
    kBlue,
    kBrown,
    kCount
  };

  enum class Neatness {
    kState1,
    kState2,
    kState3,
    kCount
  };
};
